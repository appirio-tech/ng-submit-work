h1 Tell us which <strong>icons</strong> you prefer

ul
  li(ng-repeat="icon in vm.visualDesign.icons track by $index")
    h3 {{icon.name}}

    p {{icon.description}}

    .img

    selected-button(type="button" ng-model="icon.selected")
