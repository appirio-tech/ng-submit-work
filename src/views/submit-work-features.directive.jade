header
  include nav

  img(src="/images/features-white.svg")

  h1 Specify Features

  p Tell us what features we need to include in your new app.

.flush-height.flex.column(flush-height=true)
  ul.interactive.flex.center.middle.flex-grow.selectable-choices
    li
      img.icon.biggest(src="/images/define-feature.svg")

      h3 Define features

      p UX design greating the usability, accessibility, and costume journey.

      button.action.wide(ng-click="vm.showFeatures()") select

    li
      img.icon.biggest(src="/images/upload.svg")

      h3 Upload document

      p Upload your specs or any documents you have.

      button.action.wide(ng-click="vm.showUpload()") select

  a.button.continue.wider.action(ui-sref="submit-work-visuals({ id: vm.workId })") go to design

modal.full.define-features(show="vm.showFeaturesModal" background-click-close=true)
  h2 Data booklet mobile app <strong>features</strong>

  main.flex.flex-grow.stretch
    ul.features.flex.column
      li
        ul
          li(ng-repeat="feature in vm.features")
            button.widest.clean(ng-click="vm.activateFeature(feature)")
              img.icon.small(src="{{ feature.icon }}" ng-hide="feature.selected")

              img.icon.small(src="/images/icon-check-solid.svg" ng-show="feature.selected")

              span {{ feature.title }}

      li.flex-grow
        button.widest.clean(ng-click="vm.toggleDefineFeatures()")
          span Define a new feature

          .icon +

    ul.contents.flex.column.flex-grow
      li.flex.flex-grow
        .default.active(ng-hide="vm.showDefineFeaturesForm" ng-class="{active: !vm.activeFeature}")
          h4 Select and define features for your app

          p Please list the functionality that your app needs to have by selecting from the list of popular features on the left or by defining your own custom features

        .description(ng-hide="vm.showDefineFeaturesForm" ng-class="{active: vm.activeFeature}")
          h4 Select and define features for your app

          h5 {{ vm.activeFeature.title }} description

          p {{vm.activeFeature.description}}

          textarea.widest(placeholder="Notes..." ng-model="vm.activeFeature.notes")

          button.wider.action(ng-if="!vm.activeFeature.selected" ng-click="vm.applyFeature()") apply feature

          button.wider.action(ng-if="vm.activeFeature.selected" ng-click="vm.removeFeature()") remove feature

          button.wider.action(ng-if="vm.activeFeatureChangedNotes(vm.activeFeature)" ng-click="vm.saveNotes()") save notes

        form.new-feature(ng-submit="vm.addCustomFeature()" ng-class="{active: vm.showDefineFeaturesForm}")
          h4 Define a new feature

          label New feature title

          input.widest(type="text" ng-model="vm.customFeature.title" required=true)

          p.error(ng-if="vm.featureTitleError") This feature name already exists, please try another.

          label New feature description

          textarea.widest(ng-model="vm.customFeature.description" required=true)

          button.wide.action(type="submit") add

          button.wide.cancel(type="button" ng-click="vm.hideCustomFeatures()") Cancel

        .example.flex-grow
          img(ng-if="vm.activeFeature && !vm.activeFeature.custom" src="/images/previews/{{ vm.activePreview }}.png")

          img(ng-if="vm.activeFeature && vm.activeFeature.custom" src="/images/previews/Custom-feature.png")

          img(ng-if="!vm.activeFeature" src="/images/previews/Default-preview.png")

      li.flex.center.space-between
        .count {{vm.selectedFeaturesCount}} features added

        button.wider.action(ng-click="vm.save()") Save

modal.upload-documents(show="vm.showUploadModal" background-click-close=true)
  .upload
    .icon.upload

    h2 Upload <strong>documents</strong>

    p Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

    ap-uploader(
      config="vm.uploaderConfig"
      uploading="vm.uploaderUploading"
      has-errors="vm.uploaderHasErrors"
    )


