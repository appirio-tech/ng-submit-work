header
  .house
    .icon.checkmark.biggest

  h1 Specify Features

  p Tell us what features we need to include in your new app.

ul.path-choices
  li
    .icon

    h3 Define features

    p UX design greating the usability, accessibility, and costume journey.

    button.action.wide select

  li
    .icon

    h3 Upload document

    p Upload your specs or any documents you have.

    button.action.wide(ng-click="vm.showUpload = true") select

button.wider.action(ng-click="vm.showFeatures = true") go to design

modal.define-features(show="vm.showFeatures" background-click-close=true)
  h2 Data booklet mobile app <strong>features</strong>

  main
    ul.features
      li
        ul
          li(ng-repeat="feature in vm.defaultFeatures track by $index")
            button.widest.clean
              .icon

              span {{ feature.name }}

      li
        button.widest.clean(ng-click="vm.showDefineFeatures = true")
          span Define a new feature

          .icon +

    ul.contents
      li
        .default.active
          h4 Select and define features for your app

          p Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

        .description
          h4 Select and define features for your app

          h5 {{ feature.name }} description

          p Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

          textarea.widest(placeholder="Notes...")

          button.wider.action apply feature

        form.new-feature(ng-submit="vm.addCustomFeature()" ng-class="{active: vm.showDefineFeatures}")
          h4 Define a new feature

          label New feature title

          input.widest(type="text" ng-model="vm.customFeature.name")

          label New feature description

          textarea.widest(ng-model="vm.customFeature.description")

          button.wider.action(type="submit") add new feature

          button(type="button" ng-click="vm.hideCustomFeatureModal()") Cancel

        .example
          .phone

      li
        .count {{vm.work.features.length}} features added

        button.wider.action next

modal.upload-features(show="vm.showUpload" background-click-close=true)
  .upload
    .icon.upload

    h2 Upload <strong>documents</strong>

    p Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

    ap-uploader(
      config="vm.featuresUploaderConfig"
      uploading="vm.featuresUploaderUploading"
      has-errors="vm.featuresUploaderHasErrors"
    )


