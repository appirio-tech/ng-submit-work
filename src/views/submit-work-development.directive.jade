header
  include nav

  .house
    .icon.warning.biggest

  h1 Development

  p Help us understand the technical requirements of your app.

modal.upload-documents(show="vm.showUploadModal" background-click-close=true)
  .upload
    .icon.upload

    h2 Upload your <strong>Development specs</strong>

    p Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

    ap-uploader(
      config="vm.uploaderConfig"
      uploading="vm.uploaderUploading"
      has-errors="vm.uploaderHasErrors"
    )

    button.wider.continue.action kick off project

.flush-height.flex.column(flush-height=true)
  ul.interactive.flex.center.flex-grow.selectable-choices
    li
      .icon

      h3 Define development specs

      button.action.wide(type="button" scroll-element="offline-access") select

    li
      .icon

      h3 Upload development specs

      button.action.wide(ng-click="vm.showUpload()") select

  button.wider.continue(type="button" scroll-element="offline-access") continue

form
  .full-height.flex.column(full-height=true)
    #offline-access.interactive.flex.column.center.flex-grow
      h2 Do you require users to have <strong>offline access to data</strong>?

      p Do your users need to be able to interact with the application when they are unable to connect to the internet (over the air or via wifi)?


      ul.or-choices.flex.center.middle
        li
          button.big.widest(
            ng-model="vm.work.offlineAccess"
            label="yes"
            value="true"
            selectable=true
            type="button"
          )

          p Users will need to interact with the app even when offline.  This feature increases complexity and costs.

        li.or
          .house OR

        li
          button.big.widest(
            ng-model="vm.work.offlineAccess"
            label="no"
            value="false"
            selectable=true
            type="button"
          )

          p The application will gracefully present a message to the user to please connect to the internet.

    button.wider.continue(type="button" scroll-element="personal-info") continue

  .full-height.flex.column(full-height=true)
    #personal-info.interactive.flex.column.center.flex-grow
      h2 Personal information

      p Is there any level of personal information? (stored or transmitted)

      ul.or-choices.flex.center.middle
        li
          button.big.widest(
            ng-model="vm.work.usesPersonalInformation"
            label="yes"
            value="true"
            selectable=true
            type="button"
          )

          p Lorem ipsum dolor sit amet, consectetur adipiscing elit.

        li.or
          .house OR

        li
          button.big.widest(
            ng-model="vm.work.usesPersonalInformation"
            label="no"
            value="false"
            selectable=true
            type="button"
          )

          p Lorem ipsum dolor sit amet, consectetur adipiscing elit.

    button.wider.continue(type="button" scroll-element="security-level") continue

  .full-height.flex.column(full-height=true)
    #security-level.security.interactive.flex.column.center.flex-grow
      h2 What level of <strong>security do you need</strong>?

      ul.selectable-choices.flex.center
        li
          .img

          label No security

          button(selectable=true type="button" ng-model="vm.work.securityLevel" value="vm.securityLevels.none")

        li
          .img

          label Minimal security

          button(selectable=true type="button" ng-model="vm.work.securityLevel" value="vm.securityLevels.minimal")

        li
          .img

          label Complete security

          button(selectable=true type="button" ng-model="vm.work.securityLevel" value="vm.securityLevels.complete")

    button.wider.continue(type="button" scroll-element="third-party-integrations") continue

  .full-height.flex.column(full-height=true)
    #third-party-integrations.interactive.flex.column.center.flex-grow
      h2 How many 3rd party integrations</strong>?

      p Enter the number of 3rd party integrations so we can estimate effort involved.

      input(type="number" min=1 max=6 ng-model="vm.work.numberOfApiIntegrations")

    .buttons
      button.continue.wider.save(ng-click="vm.save(false)") save

      button.contine.wider.kick-off.action(ng-click="vm.save(true)") kick off project

